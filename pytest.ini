[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=app --cov-report=html --strict-markers
markers =
    unit: Unit tests
    integration: Integration tests
    edge_case: Edge case tests
    slow: Slow running tests
